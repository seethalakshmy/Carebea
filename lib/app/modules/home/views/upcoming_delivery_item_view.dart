import 'package:flutter/material.dart';

import 'package:get/get.dart';

class UpcomingDeliveryItemView extends GetView {
  const UpcomingDeliveryItemView({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [
          Row(
            children: [

            ],
          )
        ],
      ),
    );
  }
}
